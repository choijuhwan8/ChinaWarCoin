# URL of the news proxy server. During local development it should point to the
# Express backend started via `npm run start:backend` (defaults to http://localhost:4000).
VITE_NEWS_API_BASE=http://localhost:4000
